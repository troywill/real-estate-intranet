#!/usr/bin/env perl
$| = 1;
use warnings;
use strict;

my $t1 = time;
while (1) {
    my $t2 = time;
    my $diff = $t2 - $t1;
    my $hours = $diff / 3660;
    # Round number to 2 digits after decimal point
    printf("%.2f\r", $hours);
    sleep 1;
}
